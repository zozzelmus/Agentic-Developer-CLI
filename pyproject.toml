[project]
name = "zozz-cli"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "argparse>=1.4.0",
    "google-genai==1.12.1",
    "python-dotenv==1.1.0",
]
